#ifndef __mogedDifferenceFunctionViewer__
#define __mogedDifferenceFunctionViewer__

/**
@file
Subclass of DifferenceFunctionViewer, which is generated by wxFormBuilder.
*/

#include "MogedFrames.h"

/** Implementing DifferenceFunctionViewer */
class mogedDifferenceFunctionViewer : public DifferenceFunctionViewer
{
	wxBitmap *m_bitmap;
protected:
	// Handlers for DifferenceFunctionViewer events.
	void OnPaintView( wxPaintEvent& event );
	void OnCloseButton( wxCommandEvent& event ) ;
	
public:
	/** Constructor */
	mogedDifferenceFunctionViewer( wxWindow* parent, int from_dim, int to_dim,
								   const float* error_values, 
								   float error_threshold,
								   const char* from_name,
								   const char* to_name);
	~mogedDifferenceFunctionViewer();
};

#endif // __mogedDifferenceFunctionViewer__
